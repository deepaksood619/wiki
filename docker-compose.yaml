version: "3.7"

services:
    wiki:
        stdin_open: true
        tty: true
        build:
            context: .
            dockerfile: Dockerfile
        restart: always
        image: deepaksood619/wiki:latest
        container_name: wiki
        environment:
            - DEBUG=True
        ports:
            - 8000:8000
        entrypoint:
            - /bin/bash
            - -c
            - |
                sleep infinity
                # cd .layouts && npm run develop
                # npm i -f
                # npm run develop
                # npm run build
        # volumes:
        #     - .:/app/
